services:
  postgres:
    image: postgres:16-alpine
    container_name: cod_risk_db
    ports:
      - '5433:5432'
    environment:
      POSTGRES_DB: cod_risk_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
